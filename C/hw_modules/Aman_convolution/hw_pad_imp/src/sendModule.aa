//

$opaque $module [sendModule] $in (rb cb chl_out shift_val : $uint<16> num_parts : $uint<8> max_chl : $uint<16> activation : $uint<1>) $out () $is
{
	$parallelblock [sendBlock]{
		$call sendInner1 (0 rb cb chl_out shift_val num_parts max_chl activation) ()
		$call sendInner2 (($bitcast ($uint<32>) (chl_out*cb)) rb cb chl_out shift_val num_parts max_chl activation) ()
	}
}

#define num 1
#include sendInner.aa
#define num 2
#include sendInner.aa
