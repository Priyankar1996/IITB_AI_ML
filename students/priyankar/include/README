There are 3 functions, used in this whole process of creating a tensor.They are:

1. fillTensorDescriptor() : Takes input from the user about the tensor to be 
        created.

2. printTensorDescriptor() : This function displays the tensor's description to
        the user.

3. createTensor() : This function allocates pages to the tensor in the memory pool 
        according to its dimensions. Returns '0' if it is executed properly, else '1'.